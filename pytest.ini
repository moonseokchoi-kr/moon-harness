[pytest]
# Wave 1 코어 오프라인 단위 테스트 스위트 (F20/F21)
# - testpaths: `pytest` 단독 실행 시 tests/ 만 대상
# - addopts: 기본 출력 간결화
# - 라이브 eval (evals/) 은 이 스위트에 포함되지 않음 (F21: 분리 유지)
testpaths = tests
addopts = --tb=short
markers =
    offline: 네트워크/LLM 호출이 없는 순수 오프라인 테스트 (CI blocking suite)
